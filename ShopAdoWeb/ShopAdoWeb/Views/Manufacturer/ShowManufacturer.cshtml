@using ShopAdoWeb.Models
@model IEnumerable<Manufacturer>
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <link href='@Url.Content("~/Content/bootstrap.min.css")' rel="stylesheet" type="text/css" />
    <script src='@Url.Content("~/Scripts/bootstrap.min.js")'></script>
    <title>ShowManufacturer</title>
</head>
<body>
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
            </tr>
        </thead>
        <tbody>
            @foreach (Manufacturer man in Model)
            {
                <tr>
                    <td>@man.ManufacturerId</td>
                    <td>@man.ManufacturerName</td>
                </tr>
            }
        </tbody>
    </table>
    <a id="MainContent_ctl00_SomeLinkButton"
       class="btn btn-danger btn-sm"
       href="/Good/ShowGoods">Goods</a>
    <a id="MainContent_ctl00_SomeLinkButton"
       class="btn btn-warning btn-sm"
       href="/Category/ShowCategory">Categories</a>
</body>
</html>
